# R
.Rhistory
*.RData
*.Rproj.user
.Rproj.user

# Data and output (large/generated files)
output
data

# Secrets
private_keys.json

# Web app (Node/Vite)
web/node_modules
web/dist
web/.wrangler
# Note: web/public/data/ (R-exported map assets) is already covered by the "data"
# rule above. It is deployed via `wrangler pages deploy` from web/dist, not git.

# OS
.DS_Store
