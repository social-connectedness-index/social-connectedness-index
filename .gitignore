# R
.Rhistory
*.RData
*.Rproj.user
.Rproj.user

# Data and output (large/generated files)
output
data

# Secrets
private_keys.json

# Web app (Node/Vite)
web/node_modules
web/dist
web/.wrangler
# Local env files (hold the Mapbox token for the Interactive Explorer — kept out
# of git; the deployed token is embedded at `vite build` time from .env.local)
web/.env
web/.env.local
web/.env.*.local
# Note: web/public/data/ (R-exported map assets) is already covered by the "data"
# rule above. It is deployed via `wrangler pages deploy` from web/dist, not git.

# OS
.DS_Store
