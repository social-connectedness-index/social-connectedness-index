{
  "name": "sci-map-web",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Static, browser-rendered Social Connectedness Index map maker",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "deploy": "vite build && npx wrangler@latest pages deploy dist --project-name social-connectedness"
  },
  "dependencies": {
    "jspdf": "^2.5.2",
    "mp4-muxer": "^5.1.3",
    "svg2pdf.js": "^2.2.4"
  },
  "devDependencies": {
    "vite": "^5.4.10"
  }
}
